const { locales } = require('../config')

module.exports = function (app) {
  // const ds = app.dataSources
  
  // ds.define('LocaleString', locales.reduce((acc, locale) => {
  //   acc[locale] = String
  //   return acc
  // }, {}))

  // ds.define('BodyPart', {
  //   type: String,
  //   props: Object
  // })

  // ds.define('BodyConstructor', {
  //   checksum: String,
  //   parts: ['BodyPart']
  // })

  // ds.define('LocaleBodyConstructor', locales.reduce((acc, locale) => {
  //   acc[locale] = 'BodyConstructor'
  //   return acc
  // }, {}))
}
